dotfiles
========

Configuration files for terminal. Use bash and have vim installed

To correctly set up this dotfiles, simply run `./install.sh` and it will create
a backup of your current configuration and create a symlink to the repos files
in your `$HOME` folder.
